type: object
title: PunishmentCreatedEvent
description: >
  Represents an event indicating that a punishment was created.
  Requires the `stream:read` and `punishments:read` scopes to receive this event.
required:
  - data
properties:
  data:
    type: object
    title: PunishmentCreatedEventData
    description: The data associated with the punishment.
    required:
      - punishment
    properties:
      punishment:
        $ref: "../../openapi.yaml#/components/schemas/Punishment"
