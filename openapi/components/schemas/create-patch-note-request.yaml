type: object
required:
  - target
  - category
  - title
  - body
properties:
  target:
    $ref: "./patch-note.yaml#/properties/target"

  category:
    $ref: "./patch-note.yaml#/properties/category"

  title:
    $ref: "./patch-note.yaml#/properties/title"

  body:
    $ref: "./patch-note.yaml#/properties/body"

  authorId:
    $ref: "./patch-note.yaml#/properties/authorId"

  images:
    type: array
    description: The image files attached to the patch note.
    items:
      type: string
      format: binary
