type: object
required:
  - id
  - target
  - category
  - title
  - body
  - imageUrls
  - createdAt
properties:
  id:
    $ref: "../../openapi.yaml#/components/schemas/PatchNoteId"

  target:
    $ref: "../../openapi.yaml#/components/schemas/PatchNoteTarget"

  category:
    $ref: "../../openapi.yaml#/components/schemas/PatchNoteCategory"

  title:
    type: string
    description: Title of the patch note.

  body:
    type: string
    description: Body text of the patch note.

  imageUrls:
    type: array
    description: URLs of images attached to the patch note.
    items:
      type: string
      format: uri

  createdAt:
    type: string
    format: date-time
    description: Date and time when the patch note was created.
