type: object
title: FriendRemovedEvent
description: >
  Represents an event indicating that a friendship was removed.
  Requires the `stream:read` scope and either the `players:read` or `players:read-details` scope to receive this event.
required:
  - data
properties:
  data:
    type: object
    title: FriendRemovedEventData
    description: The data associated with the friendship.
    required:
      - player
      - friend
    properties:
      player:
        $ref: "../../openapi.yaml#/components/schemas/Player"
      friend:
        $ref: "../../openapi.yaml#/components/schemas/Player"
