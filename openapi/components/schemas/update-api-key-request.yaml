type: object
additionalProperties: false
minProperties: 1
properties:
  name:
    $ref: "./api-key.yaml#/properties/name"

  scopes:
    $ref: "./api-key.yaml#/properties/scopes"

  expiresAt:
    $ref: "./api-key.yaml#/properties/expiresAt"

  playerId:
    $ref: "./api-key.yaml#/properties/playerId"
