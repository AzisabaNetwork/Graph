services:
  swagger-ui:
    image: swaggerapi/swagger-ui
    ports:
      - "8080:8080"
    environment:
      SWAGGER_JSON_URL: /spec/openapi.yaml
    volumes:
      - ./:/usr/share/nginx/html/spec:ro
