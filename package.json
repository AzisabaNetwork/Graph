{
  "name": "graph",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "openapi:lint": "redocly lint openapi/openapi.yaml",
    "typecheck:sdk-tools": "tsc --noEmit -p tsconfig.json",
    "generate:server": "rimraf server/generated && openapi-generator-cli generate -c server/generator-config.yaml",
    "generate:java-sdk": "rimraf sdks/java/generated && openapi-generator-cli generate -c sdks/java/config.yaml && node sdks/java/postprocess.ts",
    "generate:rust-sdk": "rimraf sdks/rust/generated && openapi-generator-cli generate -c sdks/rust/config.yaml && node sdks/rust/postprocess.ts",
    "generate:typescript-sdk": "rimraf sdks/typescript/generated && openapi-generator-cli generate -c sdks/typescript/config.yaml && node sdks/typescript/postprocess.ts"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.39.1",
    "@redocly/cli": "^2.38.0",
    "@types/node": "^24.0.0",
    "rimraf": "^6.1.3",
    "typescript": "^5.9.3"
  }
}
